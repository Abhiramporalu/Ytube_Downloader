{
  "name": "ytube-downloader",
  "version": "1.0.0",
  "description": "YouTube downloader backend and frontend monorepo",
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build && cd ../backend && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_linux -o yt-dlp && chmod a+rx yt-dlp",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "ffmpeg-static": "^5.3.0"
  }
}
